# Catch Blue — Implementation Plan

Companion to `game_design.md`. Covers technology choice, proposed architecture, and a time-boxed milestone plan for Personal Project 1 on the boot.dev DevOps path.

## 0. Status and purpose *(updated 2026-09-20)*

**The boot.dev deliverable is done.** As far as the DevOps path is concerned, Catch Blue was complete once the pixel theme and the first real question set shipped — M6 closed 2026-09-07, and the browser build went live on GitHub Pages on 2026-09-09. M7 (settings, pause, back buttons, the graded bank, the tier policy) closed 2026-09-19 on top of that; the suite stands at 555 headless tests and the bank at 2,603 graded questions across 21 topic files.

**The project continues on the side.** David is carrying it forward outside the boot.dev clock to add three new modes named in the GDD: **Run from Red**, **Race Green**, and **Find Yellow**. Run from Red is one mode now — the earlier Escape/Marathon split is gone, and every reference to it below is marked superseded. Two consequences for the rest of this document: the 30-hour gate in §5 is moot — it was passed and then retired at M6, and there is no longer a deadline to measure against — and Phase 2 is the current work rather than a stretch goal. The Phase 1 sections below are kept as the record of how the shipped game was built; the live planning is in §5 *Phase 2* and §7, and each new mode gets its own `milestones/` break-out when it starts, same as M0–M7 did.

Standing items carried across the boundary, all David's call on timing: character sprites (waiting on the artist — spec in `milestones/m6.md` M6.f.4), and the question-bank content work (the rewrite queues and tracker loose ends in `question_bank_tracker.md`, plus the five Vol. 2 topics not yet vetted). Neither blocks a new mode.

**The pre-Phase-2 refactor is done (2026-09-20).** Before starting Run from Red, David asked for a structural review of the game scripts; it is `claude_refactor_suggestions.md` in the repo root, revised the same day after a second review and after three movement decisions (Race Green is forward-only, its tier distance is rows-to-top, and it gets a five-row camera from the bottom of the strips). Steps 1–4 of its §13 landed that day, each verified against the M7 galleries byte for byte: a pygame-free `Match` owning turn accounting (`match.py`); `QuestionPopup` and `CellTopics` extracted from the play state; `Board.distance` and catches through `board.neighbors`, with one shared step-chooser (`characters.best_step`) proven equal to Blue's old flee over 47,160 seeded cases; and a mode registry (`modes.py`) with a `Rules` protocol (`rules.py`) that `PlayState` consults everywhere Catch Blue's rules used to be inline. `game.bank` replaced the bank parameter on every state; per-mode preset tables replaced `PRESETS`; the end screen inherits the play renderer. The suite was re-swept to the new surface (567 tests). Run from Red is step 5, broken out in `milestones/m8.md`.

## 1. Where you're starting from

By this point in the DevOps path you've completed: Python basics, Bookbot (a CLI project), Linux + shell scripting, Git, Object-Oriented Programming, and **Build an Asteroids Game — which is built with Pygame**. That last one matters a lot: you've already written a Pygame game loop, handled events, drawn to a screen, and organized game objects as classes. Catch Blue is *mechanically simpler* than Asteroids (turn-based, no physics, no delta-time movement), so the new work is mostly UI (menus, question popups, text) and game-state management, not rendering fundamentals.

## 2. Framework options

| Option | What it is | Pros | Cons |
|---|---|---|---|
| **Pygame** ⭐ | The library you used for Asteroids | You already know it; huge community; handles grid + mouse clicks easily; path to web (pygbag) and .exe (PyInstaller) later | No built-in UI widgets — you write your own buttons/text boxes |
| Terminal / TUI (plain `input()` or Textual) | Text-based version in the terminal | Fastest to build (could halve the estimate); very DevOps-flavored; trivial to test | No real "clicking a grid"; feels like a step back from Asteroids; menus in Textual are their own learning curve |
| Arcade | Modern Pygame alternative (OpenGL-based) | Cleaner API, nice docs | New library to learn for no real benefit at this scale |
| Pyxel | Retro fantasy-console engine | Charming aesthetic, small API | 16-color/lo-fi constraints fight against text-heavy flashcards |
| tkinter | Python's built-in GUI toolkit | Ships with Python; buttons/text entry are *built in* (great for quizzes) | Feels like a form app, not a game; skills don't build on Asteroids |
| Godot / Ren'Py etc. | Full engines | Powerful | Not Python (GDScript) or wrong genre; defeats the "apply my Python" goal |

**Recommendation: Pygame.** It's the only option where week one is spent building the game instead of learning a tool, and it directly reinforces what Asteroids taught. The one honest caveat: Pygame has no widget library, so question popups, buttons, and (especially) fill-in-the-blank text input are things you build yourself. That's good OOP practice, but it's where your hours will go — see §6.

If you'd rather keep this project small and lean into the DevOps identity, the terminal version is the legitimate alternative — but given the GDD's click-a-square design, Pygame is the better fit.

**Settled in M0: the flavor is `pygame-ce`, not upstream `pygame`.** pygame-ce is the community fork that most of the active maintainers moved to in 2023. It's a drop-in — you still write `import pygame`, and every pro/con above holds unchanged. The deciding factor was Python support: upstream's last release is 2.6.1 (Sept 2024), shipping wheels only through cp313, while this machine runs Python 3.14. pygame-ce 2.5.8 ships cp314 wheels and releases on a regular cadence. One rule: never install both into the same venv — they claim the same `pygame` import name and will clobber each other.

## 3. Recommended stack

- **Python 3.14** with `pygame-ce==2.5.8` (see §2), in a `uv`-managed venv. Runtime deps live in `requirements.txt`; test-only deps belong in `requirements-dev.txt`, so the eventual container doesn't ship a test runner. Note the venv is uv-created and therefore has no `pip` — install with `uv pip install <pkg>`, not `python -m pip`.
- Questions stored as **JSON files** in a `data/` folder (no database needed)
- `pytest` for unit-testing the game *logic* (movement rules, answer checking) — a nice DevOps-path touch, and easy if you keep logic separate from rendering. **This is real as of M0:** `tests/test_board.py` covers the grid math and runs fully headless, because `pygame.Rect` works without `pygame.init()` and no window is ever opened. M1 added `tests/test_characters.py` — starting positions, arbitrary placement, and the shape/color contract the view draws against. M2 grew both files: adjacency geometry tables, `legal_moves` with and without blockers, and the full flee contract (steps away, hugs edges, stays when cornered, deterministic on ties, plus a 600-pairing full-board sweep) — 66 tests total, all headless. That means it runs in CI with no display attached. *(As of M7's close, 2026-09-19: 555 tests across 21 files, still fully headless; the pure-logic guards now cover `board`, `game_setup`, and `questions`, each imported in a subprocess and checked for a pygame-free `sys.modules`. Run with `SDL_VIDEODRIVER=dummy` so the few tests that render do so off-screen.)* `pytest.ini` sets `pythonpath = .` so `tests/` can import top-level modules without any packaging ceremony.
- Distribution: just run it as `.py` for the project submission. Later, `PyInstaller` makes an executable and `pygbag` compiles Pygame to WebAssembly for the browser — both are post-MVP, and this answers the "web app vs executable" unknown in the GDD: **defer it; the code doesn't change either way.** *(Resolved 2026-09-09: the game runs in the browser at davhardin.github.io/catch_blue, compiled by `pygbag` and published to GitHub Pages by the Actions workflow in `.github/workflows/pages.yml` on every push to `main`. The code changed less than feared — `pygbag.ini` ignore lists, `SCALED` switched off under emscripten, and a canvas refit after `set_mode` — and the `.py` entry point still runs natively. PyInstaller remains unplanned.)*

## 4. Proposed architecture

### Scene/state machine

The biggest structural difference from Asteroids: Catch Blue is a sequence of *modes*, not one continuous loop. Model it as a state machine where each state owns its own event handling and drawing:

```
MENU ──► BOARD ──► QUESTION ──► BOARD ──► ... ──► GAME_OVER ──► MENU
```

One top-level `Game` class runs the loop and delegates to the current state object. Clicking a topic square switches `BOARD → QUESTION`; answering switches back and applies the result per the turn rules below.

*(Revised 2026-08-31, settled in `milestones/m4.md`: MENU is three screens — Game Select → Subject → Topics — and QUESTION stays an overlay `if` inside the play state rather than a sibling state, since the popup overlays a live board instead of replacing it. Five states total: GAME_SELECT, SUBJECT, TOPICS, PLAY, GAME_OVER.)*

### Turn and question rules (settled 2026-08-25)

- **Movement is 4-way orthogonal for every character.** Player, Blue, and later Red move only left, right, up, or down, one square, within the board. `is_adjacent` / `get_distance` in `board.py` are the single definition (built in M2 — see `milestones/m2.md`). *(Revised 2026-09-20: movement is a board property now — `Board.neighbors` and `Board.distance` replaced the free `is_adjacent` / `get_distance` in the refactor, and catches go through `neighbors` too. Catch Blue, Run from Red, and Find Yellow stay 4-way; Race Green is forward-only (up, up-left, up-right), which will be a different `offsets` tuple on its board — GDD §3.)*
- **Answer outcomes are asymmetric.** Correct answer: the player moves onto the clicked square; Blue holds still. Incorrect answer: the player stays put; Blue flees. Exactly one side moves per question — the player's progress and Blue's movement are both rationed by answer quality.
- **Catching is question-gated too** *(decided 2026-08-26)*. Clicking adjacent Blue opens a question drawn from the category of Blue's square; correct completes the catch, incorrect gets the standard wrong-answer outcome — the player stays and Blue flees the fumbled grab, breaking adjacency. A playtesting-era evolution is queued in §7: Blue's square temporarily holding a random topic while Blue stands on it.
- **Red's movement trigger is undecided.** Move-on-incorrect-only (mirroring Blue) makes Red gentle; the GDD's original "toward the player every turn" makes Red relentless — very different difficulty curves. Deliberately deferred: decide from game balance once Catch Blue mode is fully playable, not before. *(Settled 2026-09-20 in the GDD, David: Red moves toward the Player on incorrect answers only, mirroring Blue. Green follows the same rule in Race Green, with Medium/Hard possibly changing it.)*
- **Squares have persistent categories; questions rotate.** At game start each square is assigned a question category, fixed for the whole game. The *question* shown for a square is not fixed: once the player answers it — correct or not — and moves on, revisiting that square draws a fresh question from the same category. Implications for M3: the cell → category mapping is game state assigned at setup (not `Board` geometry), and `QuestionBank` needs "give me an unused question from category X" plus a policy for what happens when a category runs dry (recycle oldest is the likely answer — decide in M3). *(Revised 2026-08-31: categories are now two-level — chapter-scale `topic` plus section-scale `subtopic` in the question JSON. Squares are labeled with **subtopics**, drawn visibly on the board so choosing a square is an informed decision; the future menu selects which *topics* are in play; the bank rotates within (topic, subtopic) pools. See `milestones/m3.md` for the full settlement.)*

### Classes (maps to GDD §4)

- `Game` — window, clock, main loop, state switching
- `Cell` — a `NamedTuple` of `(col, row)`. Unpacks and hashes like a plain tuple, but `.col` / `.row` at call sites kills a whole class of transposition bug. *(Built in M0.)*
- `Board` — grid model, **pure logic, no pygame import**. Holds `cols` / `rows`; provides `in_bounds(col, row)` and `cells()`, a generator yielding every `Cell` col-major so the nested loop is written exactly once. *(Built in M0. Adjacency landed in M2 as free functions `is_adjacent` / `get_distance` plus `Board.neighbors`; occupancy is handled as explicit blocked-cell sets passed to the rule functions, not board state — see `milestones/m2.md`.)*
- `BoardView` — everything pixel-shaped: `cell_size`, `origin_x` / `origin_y`, `cell_to_rect(cell)`, `pixel_to_cell(x, y) -> Cell | None`, and `draw(screen)`. Holds a reference to its `Board`; the board knows nothing about the view. *(Built in M0; M1 added entity drawing — dispatch on `shape` — plus hover and selection highlights. M2 added the legal-move tint, fed the same set the click handler validates against — main computes, view paints.)*
- `Character` (base) → `Player`, `Blue` — *(Built in M1, superseding the planned `NPC` base: with two concrete classes in hand the shared surface was visible — a `cell`, plus `shape` and `color` as class-attribute defaults the subclasses override. Pygame-free like `board.py`; `BoardView` renders by dispatching on `shape`. See `milestones/m1.md`.)* Starting positions live in per-subclass `at_start(board)` classmethods — Player in a corner, Blue in the center as of M2 (a fleeing character's real resource is distance-to-walls, and the corner start had Blue beginning in its own checkmate square). Movement (`move_to`, `legal_moves`) and Blue's flee landed in M2: `flee_step` is a pure chooser returning a `Cell` — the caller applies it through `move_to` — greedy, deterministic on ties, and holding still when cornered. Greedy proved enough; no pathfinding on an open grid. `Red` (moves *toward* the player on incorrect answers only — settled 2026-09-20, see the turn rules below) joins as a subclass in Phase 2, with `Green` and `Yellow` after it. **Note for Run from Red:** that mode has only two characters — Player and Red, with the Player taking Blue's fleeing role. So the Player will need to start in the *center* (Blue's spot) when the user selects Run from Red, not the corner: `at_start` as written hard-codes one position per subclass, so Phase 2 needs a way to set the Player's start by mode (parameterize `at_start`, or have the mode/state pass the start cell). Decide the mechanism when Run from Red lands. *(Refactor, 2026-09-20: `at_start` is gone — start cells come from the mode's `Rules.start_cells(board, rng)`; Blue's flee is a one-line call to the shared `best_step(board, from_cell, target, rng, *, prefer, blocked)`, and Red's chase is the same call with `prefer=lt` and an empty blocked set, per `milestones/m8.md`.)*
- `Question` / `QuestionBank` — load JSON, filter by (topic, subtopic) pair and difficulty, track which questions were used. `subject` and `topic` are snake_case identifiers forming the in-data hierarchy subject → topic → subtopic; `subtopic` is display text drawn verbatim on the board *(hierarchy settled 2026-08-31 — see `milestones/m3.md` and `m5.md`)*. Carries the optional `shuffle` opt-out flag; the *display* order of choices is the play state's business, not the data's (M5.e)
- UI helpers — `Button`, `TextBox` (word-wrapped text rendering), later `TextInput` for fill-in-the-blank
- *(Added 2026-09-20 in the pre-Phase-2 refactor, all pygame-free except the popup:)* `Match` (`match.py`) — board, Player, NPC, the move counter, and `resolve(is_correct, target, intent)`; `Rules` (`rules.py`, a `Protocol`) — one class per mode answering the ten questions `PlayState` used to answer inline (board, starts, blockers, click intent, question cell, pressure distance, the two answer handlers, outcome, menu eligibility) with `CatchBlueRules` as the first; `Mode` / `MODES` (`modes.py`) — the registry Game Select and Settings are built from, each entry carrying its own preset table (`ModeSettings`) and marked active only when it has rules and presets; `CellTopics` (`cell_topics.py`) — the cell → (topic, subtopic) map and its exhausted-pool relabeling; `QuestionPopup` (`question_popup.py`) — the prompt, answer buttons, reveal, and Continue as one object the play state holds or not.

### Grid and screen layout (settled in M0)

**Window: 1280×720, created with the `pygame.SCALED` flag.** 720p fits every laptop panel including old 1366×768 ones, matches what Asteroids used, and leaves a natural side column for HUD. `SCALED` means all code works in fixed 1280×720 logical coordinates while pygame scales the output to the real display — so HiDPI monitors and a future fullscreen toggle cost nothing in layout math. *(Superseded 2026-09-07: the canvas is 1600×900 with an 800 px board region, for readability — labels and popup text were too small for students at 720p, and most student laptops are large enough. See `milestones/m6.md` M6.e log.)*

**Fixed board *region*, derived cell size.** The board occupies a square 640×640 region at origin (40, 40); `cell_size = BOARD_REGION // max(cols, rows)`, and any leftover pixels are absorbed by centring the board inside the region. This is what lets one `BoardView` serve Catch Blue's 5×5 (128px cells, no slack) and Run from Red's 9×9 (71px cells, 1px slack) without the window resizing between modes and without every HUD coordinate moving. For a board larger than the screen — Race Green's tall strips and Find Yellow's maze, as of the 2026-09-20 GDD — the same region becomes a viewport: hold `cell_size` fixed and scrolling is an offset change rather than a layout rewrite.

**Coordinate order is `(col, row)`, everywhere, forever.** Integer division must use `//`, never `int(x / y)`: `//` floors, so a click left of or above the grid yields a negative index that `in_bounds` rejects, whereas `int()` truncates toward zero and would silently land that click in column 0. Bounds are checked *after* dividing, reusing `Board.in_bounds` rather than a second pixel-space guard.

All layout numbers live in `constants.py` — one definition each, so changing `BOARD_REGION` moves everything consistently.

### Question data format

```json
{
  "id": "bio-042",
  "subject": "anatomy_physiology",
  "topic": "cells",
  "subtopic": "Organelles",
  "difficulty": 2,
  "type": "multiple_choice",
  "prompt": "Which organelle is the site of ATP synthesis?",
  "choices": ["Nucleus", "Mitochondrion", "Ribosome", "Golgi apparatus"],
  "answer_index": 1
}
```

For fill-in-the-blank, use `"type": "fill_blank"` with an `"accepted_answers"` list, and normalize input (lowercase, strip whitespace) before comparing. **Start with multiple-choice only** — clickable answer buttons are far easier in Pygame than a text-input widget, and you can add `fill_blank` as its own milestone.

**Choice order is a display concern, not a data concern** *(settled 2026-09-01 — see `milestones/m5.md` M5.e)*. The game shuffles the choices at ask time, so the stored order is canonical and never load-bearing: new questions may be written answer-first (`answer_index: 0`), which makes files auditable at a glance, and no hand-scrambling ever risks an `answer_index` that silently points at the wrong choice. Questions with positional choices ("all of the above") opt out with an optional `"shuffle": false` field; absent means shuffle.

### Suggested file layout

Files marked ✅ exist; the rest are still planned.

```
catch_blue/
├── main.py            ✅ thin entry point (window init + Game handoff, ~20 lines since M4)
├── constants.py       ✅ screen + board geometry, colors, label styling
├── board.py           ✅ pure logic: Cell, Board
├── board_view.py      ✅ pixel conversions + drawing, incl. subtopic labels
├── game.py            ✅ Game class + state machine  (M4; holds `bank` and per-mode `settings_by_mode` since the 2026-09-20 refactor)
├── states/            ✅ menus.py, play.py, game_over.py  (M4; M5 adds Topics scrolling, answer shuffle; play.py is the generic turn loop since 2026-09-20 — rules, match, popup, and cell map live outside it)
│                         settings.py, pause.py  (M7 — seven states: GAME_SELECT, SUBJECT, TOPICS, SETTINGS, PLAY, PAUSE, GAME_OVER)
├── game_setup.py      ✅ pure helpers: topic prettify, display-name tables, Settings + GameConfig  (M4/M7; presets moved to modes.py and cell assignment to cell_topics.py, 2026-09-20)
├── characters.py      ✅ Character, Player, Blue, best_step  (Red lands in M8)
├── match.py           ✅ Match — pure turn state and resolve()  (refactor step 1, 2026-09-20)
├── rules.py           ✅ Rules protocol, Intent/Outcome enums, CatchBlueRules  (refactor step 4; RunFromRedRules in M8)
├── modes.py           ✅ Mode registry, ModeSettings preset tables  (refactor step 4)
├── cell_topics.py     ✅ CellTopics — cell→pair map + exhausted-pool relabeling  (refactor step 2)
├── question_popup.py  ✅ QuestionPopup + AnswerReveal — the question overlay as one object  (refactor step 2)
├── questions.py       ✅ Question, QuestionBank  (M3: subtopics, pair-scoped rotation; M5: load guards, subject, shuffle flag; M6: seeded shuffle; M7: pool cycling, tier walk)
├── ui.py              ✅ Button, TextBox, Checkbox, option rows  (widgets describe; render.py draws)
├── render.py          ✅ Renderer — the only module that calls pygame.draw / blit for widgets  (M6.f)
├── theme.py           ✅ flat and pixel themes as data: nine-slice specs, fonts, per-board-size label sizes  (M6.f)
├── data/questions/    ✅ one .json per topic — 21 topic files, 2,603 questions, all graded 1–3  (M5 split; M5–M7 treadmill)
├── tests/             ✅ 21 files, 567 tests (board, characters, questions, game_setup, play, reveal, pool cycling, tier policy, theme/render/skin, menus, M7 flow, ...) — re-swept 2026-09-20 to the refactored surface
├── tools/             ✅ gallery.py (renders every screen in both themes to PNG), theme_fit.py (label overflow check per board size)
├── .github/workflows/ ✅ pages.yml — pygbag build + GitHub Pages deploy on push to main  (2026-09-09)
├── pygbag.ini         ✅ web-build ignore lists
├── pytest.ini         ✅ pythonpath = .
├── .gitignore         ✅
├── requirements.txt   ✅ runtime only
└── requirements-dev.txt  ✅ pytest (added 2026-09-06)
```

`board_view.py` and `constants.py` weren't in the original layout — they fell out of the M0 decision to keep pixels out of `board.py`. `render.py` and `theme.py` fell out of M6.f the same way: the visual pass needed one place that draws and one place that describes, so widgets stopped drawing themselves.

## 5. Milestones and time estimates

Estimates assume you're writing most code yourself, are new-ish to coding, and include the debugging time that always comes with "first time building X." Honest range, not best case.

### Phase 1 — Catch Blue MVP

| Milestone | Deliverable | Hours |
|---|---|---|
| ~~M0~~ ✅ | Repo, venv, window opens, empty 5×5 grid drawn — *plus* the pixel↔cell conversions and 25 passing tests | 1–2 |
| ~~M1~~ ✅ | Click → cell with hover + selection highlights; `Character` base class; Player + Blue drawn at opposite corners; 36 tests total | 3–5 |
| ~~M2~~ ✅ | Click-to-move with legal-move highlights, catch detection (`print` for now), Blue's greedy flee behind a debug key, Blue's start moved to center; 66 tests total | 4–6 |
| ~~M3~~ ✅ | Question system: JSON loading, question popup with clickable multiple-choice answers, correct/incorrect flow — *expanded 2026-08-31:* topic/subtopic hierarchy and subtopic labels drawn on the board; 96 tests total | 8–14 |
| ~~M4~~ ✅ | Game flow: state machine, three-screen menu wizard (Game Select → Subject → Topics), win condition (catch adjacent Blue through its question — see §4), 20-move loss, end screens with replay — *scope settled 2026-08-31, see `milestones/m4.md`*; 113 tests total | 8–12 |
| ~~M5~~ ✅ | Content: the real question bank — deep (8+ per subtopic, 200+ questions), curated by David from his study materials and openly licensed banks (CC-attributed — see the curation decision in `milestones/m5.md`), difficulty-tagged 1–3 (tag shipped in the format; grading deferred to playtesting, see §7) — plus data-layer hardening for authoring at scale, the `subject` field (settled, see §7), per-topic file split, runtime answer-choice shuffling before any import (files stay canonical — see §4 and `milestones/m5.md` M5.e), and the Topics screen learning to scroll (list scrolls, Start button stationary). *Scope settled 2026-08-31, shuffle added 2026-09-01; engineering half largely landed as of the 2026-09-01 commit — see `milestones/m5.md`* | 10–16 |
| ~~M6~~ ✅ | Polish: playtesting, bug fixes, visual cleanup — *scope settled 2026-09-06, see `milestones/m6.md`:* the mechanical §7 knobs first (randomized question rotation, randomized flee tie-breaks, the answer reveal — all shipped 2026-09-06; Blue's-square possession was scoped in, then declined), then a playtest-and-tune pass, then a style decision and one timeboxed visual pass. Roughly 10–15 build hours as scoped; each sub-goal ships independently | 4–8 → 10–15 |
| ~~M7~~ ✅ | Settings, flow, and the graded bank — *added 2026-09-06, scope below; break-out doc `milestones/m7.md` written 2026-09-07 when M6 closed.* A Settings screen with Difficulty presets and a board-size toggle; wizard back buttons, an in-game pause screen, and a click-to-continue on wrong answers; the question audit finished, every question difficulty-graded, and difficulty behavior wired to the chosen preset. *Closed 2026-09-19: a–f shipped; sprites and the remaining bank content work deferred together to a later pass (David).* | 10–16 |
| | **Total** | **~48–79** *(M7 added 2026-09-06; Phase 1 through M6 remains ~38–63)* |
| | **Phase 1 closed 2026-09-19.** Web launch 2026-09-09 sits between M6 and M7 (not a milestone — see §3 *Distribution*). | |

The GDD's gate says "only add Run from Red if Catch Blue lands under 30 hours." That's achievable if you stay multiple-choice-only, keep art to colored rectangles + text, and defer accessibility modes. If you're past 30 hours at M6, ship it — a finished Catch Blue is a better portfolio piece than a half-finished Run from Red. *(M7, added 2026-09-06, sits between that decision and Phase 2: its settings and grading work also serve Run from Red — difficulty presets and board size are the levers Run from Red needs — so building it first isn't a detour from the gate, it's the on-ramp.)* *(Retired 2026-09-20: the gate was passed at M6 and the boot.dev deliverable is complete; Run from Red now proceeds on David's own clock — see §0.)*

*(Gate accounting, settled 2026-08-31: M5's deep bank was chosen deliberately — David tutors A&P, and the game is a study resource for his students, so bank depth and question quality are the product, not scope creep. Content hours are tutoring-practice work, not engineering, so measure the 30-hour gate against build hours only. The engineering total without M5's treadmill remains inside the original envelope; the treadmill runs on its own clock and can spill past the M5 commit — see `milestones/m5.md`.)*

#### M7 — Settings, flow, and the graded bank *(added 2026-09-06)*

Features David chose to hold back from M6 so the visual pass ships first. Three groups; the break-out doc (`milestones/m7.md`) gets written when M6 closes, and the settings list is expected to grow before then.

1. **Settings menu, reached from the opening screen.** The top control is **Difficulty — easy / medium / hard / custom**. Choosing a preset writes values into the controls below it; **custom** leaves them editable. The first control below it is **Board size: 5×5 / 7×7 / 9×9** — the §7 "board size as difficulty" knob becoming a real setting, and the first time `BoardView`'s fixed-region/derived-cell-size design (§4) carries its weight in Catch Blue rather than Run from Red. More controls will be added before M7 starts; the move limit (§7) is the obvious next one. Settings ride into `PlayState` the same way the wizard's choices do — as part of the config value the menus assemble — so replays keep them and Main Menu resets nothing but the state machine.
2. **Buttons.**
   - **Wizard back buttons** — m4.md's "forward-only, add if playtesting says otherwise" comes due.
   - **A pause button, top-right during play**, opening a screen with **Main Menu / Continue / Retry**. Retry behaves exactly like Play Again: same config, fresh scramble, the same living bank still rotating (m4.md's "reset by rebuilding" rule). Pause is probably a sixth state that draws the play state beneath its panel, the way GAME_OVER already does — and the pause should stop the reveal timer, which is the first time `update` on a non-play state has a job.
   - **Click-to-continue on wrong answers.** M6.c's reveal resolves on a timer; for a *wrong* answer the popup should instead stay up — the chosen button red, the correct one green — until the student clicks a **Continue** button, so they can read the right answer for as long as they need. Correct answers keep the short timed reveal. Structurally this is the reveal phase gaining a second exit (click, not clock) on one branch; the "expiry frame discards its events" rule in `milestones/m6.md` needs re-examining when the exit is a click, since that click *is* the event.
3. **Questions** *(content hours — on the treadmill's clock, not the gate's)*.
   - **Finish the audit**: the 177-entry rewrite queue and the loose ends in `question_bank_tracker.md` (Keep-range collisions, T/F sweeps, placements, the abdominopelvic-regions gap, the thin Aging subtopics).
   - **Grade every question 1–3** per the M5 rubric (1 recall, 2 application, 3 analysis) — the §7 deferral coming due, informed by the M6.e playtest notes on which questions bite.
   - **Wire difficulty behavior to the chosen Difficulty**: what easy/medium/hard *mean* for which questions are drawn — a flat tier per preset, or the §7 distance-scaled curve (3 near Blue, 2 at distance 2, 1 beyond) as the hard preset's behavior — plus the fallback when a pool has no question at the wanted tier. Depends on the grading pass above; don't build it on ungraded data.
4. **Character sprites** *(moved here from M6.f.4 on 2026-09-07 — last, because it waits on David's artist)*. Two sprites at 16 or 32 px (Blue reads as a dog, the player as a person or marker), blitted by `Renderer.sprite` from the theme's `SpriteSpec` with the existing shape draw as the fallback, plus an attribution line. The spec and the sourcing candidates are in `milestones/m6.md` M6.f.4; nothing else in M7 depends on it.

Why after M6.f rather than inside M6: the settings screen and the pause screen are two new screens the visual style has to cover, so styling first and building them *in* that style beats building them gray and restyling. The §7 entries this milestone absorbs — board size, move limit, difficulty grading, distance-scaled difficulty — stay in §7 until M7's break-out settles them.

### Phase 2 — Run from Red, Race Green, Find Yellow *(current work as of 2026-09-20; no longer a stretch goal)*

The GDD (§2, 2026-09-20) names three modes. **Run from Red** reverses Catch Blue: Player center, Red corner, Red steps toward the Player on wrong answers, and the Player wins once the moves left are fewer than the distance Red needs to close. **Race Green**: Player and Green each on a 3-wide, 9-tall strip, first to the top wins, Green advances on wrong answers. **Find Yellow**: a procedurally generated maze, Yellow hidden until found, Catch Blue's win and loss. The earlier Escape/Marathon variants are gone. Estimates below are *build-hour* guesses with nothing racing them. Each mode gets a `milestones/` break-out (M8 for Run from Red is the natural next number) written when it starts, in the same shape as M0–M7: decisions, sub-goals, traps, done-when. *(Race Green settled further the same day, GDD §2–3: forward-only movement — up, up-left, up-right, so two or three squares per turn — a five-row camera starting at the bottom of the strips so strip height can be a difficulty knob, and a tier policy measured as rows to the top.)*

**The refactor that came first (2026-09-20, §0).** The four `claude_refactor_suggestions.md` §13 steps that Run from Red depends on are done, so several bullets below are closed: the flag-or-sibling question is answered (a rules object per mode, one `PlayState` for all four), the presets are per-mode data, start cells come from the rules, and the step-toward chooser exists. The remaining genuinely-new pieces for Run from Red are `Red`, `RunFromRedRules`, its preset table, and the accent; `milestones/m8.md` breaks them out.

**What M7 already put in place for this phase:**

- **The 9×9 board is a setting, not a rewrite.** `Settings.board_size` 5/7/9 rides in `GameConfig`, the fixed-region/derived-cell-size `BoardView` handles it, and the theme carries per-size label fonts. Run from Red's grid is the 9×9 that is custom-only in Catch Blue.
- **Question difficulty is real.** Every question is graded 1–3, `QuestionBank` walks a tier filter over the one pool order, and the distance-scaled policy (hard preset) exists. Any per-mode difficulty behavior (Green speeding up on Medium/Hard, say) is a new policy in the same slot, not a new data layer.
- **Settings and presets are per-mode-ready in shape but not in data.** The preset table in `game_setup.py` is Catch Blue's; a mode will need its own table (or a mode key in the existing one), and `preset_for` must keep deriving the name from the values. *(Done 2026-09-20: `modes.py` gives each mode a `ModeSettings` — presets, default, rows — and the Settings screen rebuilds its rows per mode, adding a Game row once a second mode is active; `preset_for` moved onto `ModeSettings`.)*
- **Seven states and a config value.** Game Select already exists as the place a mode is chosen; PlayState is built from `GameConfig`. The open question is whether Run from Red is a flag on `PlayState` or a sibling state — decide in the M8 break-out. Race Green and Find Yellow, with two boards or a maze plus a camera, are more likely siblings. *(Resolved 2026-09-20: neither a flag nor a sibling — a `Rules` object per mode, consulted by one generic `PlayState`; Race Green's strips and Find Yellow's maze are `Board` variants and the camera is a `BoardView` change, so they are not siblings either. Refactor doc §2.)*

**What is genuinely new (decisions to make in the M8 break-out, not here):**

- **`Red`** as a `Character` subclass that steps *toward* the player on incorrect answers (trigger settled 2026-09-20). A step-toward chooser is `flee_step` with the comparison flipped; ties and cornering need the same treatment. *(The chooser exists: `characters.best_step` with `prefer=lt`; Red itself is M8.a.)*
- **Player start by mode.** `at_start` hard-codes one cell per subclass; Run from Red puts the Player in the center and Red at an edge or corner. Parameterize `at_start` or have the mode pass the start cells — noted in §4 *Classes*. *(Done 2026-09-20: `Rules.start_cells(board, rng)`; `at_start` is gone.)*
- **Run from Red's win and loss**: loss when Red lands *on* the Player's square (David, 2026-09-20 — so Red's `legal_moves` must not receive the Player's cell as a blocker, which is one argument left out rather than a rule added, and the draw order must put Red above the Player for the final frame); win when `moves_remaining < distance(Player, Red)` minus whatever Red can still close, the mirror of Catch Blue's early-loss rule in `play.py`. The move limit is the same counter, so no new setting.
- **The camera.** Race Green's strips and Find Yellow's maze are both taller than the screen, and Find Yellow *depends* on it to keep Yellow off-screen at the start. It is the single biggest new piece (§6 trap 3): the fixed region becomes a viewport with an offset that follows the Player, `pixel_to_cell` and `cell_to_rect` gain the offset, and `BoardView` stops assuming the whole board is visible. Build it once, in Race Green, where the board is still rectangular. *(Race Green's viewport: five rows visible, anchored at the bottom, following the Player up — GDD §2. Requirements list in refactor doc §7a.)*
- **Per-mode accent color in the pixel theme** (David, 2026-09-20): red for Run from Red, green for Race Green, yellow for Find Yellow, blue as it is for Catch Blue. Design sketch in §7.
- **Maze generation and hidden Yellow** for Find Yellow: a pure generator in the `board.py` tradition (no pygame), walls as blocked edges or blocked cells, and a fog rule for what is drawn. Design before estimating.
- **Which questions Run from Red asks.** Catch Blue asks the square you click; Run from Red is a flee, so the question may belong to the square being fled *to*, or to Red's square. Settle before writing the state. *(Default in `milestones/m8.md`: the clicked square, same as Catch Blue — `Rules.question_cell` is the one method to change if playtesting disagrees.)*

| Feature | Hours | Notes |
|---|---|---|
| Run from Red | +8–15 | `Red` + `RunFromRedRules` + its preset table on the step-4 seams; reversed win/loss; the accent (two mechanisms compared, one ships) — `milestones/m8.md` |
| Race Green | +10–18 | One 7-wide board with an unwalkable gap column, forward-only `offsets`, Green's column choice, first-to-top outcome, rows-to-top tier distance, and the **camera** (five rows from the bottom) — build it here |
| Find Yellow | +12–20 | Maze generation, hidden Yellow with a fog rule, camera reuse from Race Green; Catch Blue's win/loss otherwise |
| Fill-in-the-blank input | +3–6 | Custom Pygame text input widget; the web build needs it to work with the browser's keyboard events too |
| Sprites | +2–4 | Waiting on the artist; spec in `milestones/m6.md` M6.f.4, shape fallback stays until then |

### Explicitly deferred (post-project)

Multiplayer, colorblind/low-vision modes (do pick colorblind-safe colors from day one — it's free), packaged executables. *(Web deployment via pygbag left this list on 2026-09-09 — it shipped; see §3.)*

## 6. Scope traps to watch

1. **Text rendering in Pygame is manual.** `font.render()` gives one line, no wrapping. Budget for a small word-wrap helper early — every question popup depends on it.
2. **Fill-in-the-blank is deceptively expensive** (cursor, backspace, focus, answer normalization). That's why it's a stretch goal, not MVP.
3. **Boards larger than the screen imply a camera system.** Everything in Catch Blue and Run from Red lives on a fixed screen; Race Green and Find Yellow don't (GDD, 2026-09-20). Build the camera once, in the simpler mode, before the maze.
4. **Question authoring is a content treadmill.** Timebox it — 20–30 questions is plenty to prove the game works. *(Knowingly overridden 2026-08-31: proving the game works stopped being the goal in M4 — it's a study resource for David's A&P students now, so M5 goes deep on purpose. The 30-hour gate is measured against build hours only; see the gate accounting under the table in §5 and `milestones/m5.md`.)*
5. **Logic/rendering separation** is the difference between a testable project and an untestable one. If `board.py` never imports pygame, you can `pytest` your rules — and that's the line on this project that will impress on the DevOps path. **This is now enforced, not aspirational:** `test_board_module_never_imports_pygame` imports `board` in a subprocess and asserts `pygame` never lands in `sys.modules`. It fails the moment someone reaches for `pygame.Rect` in the wrong file.
6. **`.gitignore` before the first commit, not after.** M0 committed `__pycache__` because the ignore file didn't exist yet; untracking it needed `git rm -r --cached`, since git ignores untracked files only. Bytecode in history is harmless, but the same mistake with a `.env` is not — set this up before adding any file you'd regret.

## 7. Possible changes during playtesting

Knobs deliberately left at their simplest setting until the game is playable end-to-end. Revisit during M6 (or Phase 2 balance work) — none of these are worth tuning against an imagined player.

- **Randomized flee tie-breaks.** Blue currently breaks ties among equally-good escape cells by sorted order — deterministic, which is what makes the flee tests assertable, but learnable: a threat directly below always sends Blue *left*, never up or right. Randomizing the pick among tied candidates would make Blue feel evasive rather than mechanical. Two facts to carry into that change: equal-*distance* sidesteps are impossible (each orthogonal step changes Manhattan distance by exactly ±1 — see the corrected note in `milestones/m2.md` M2.e), so ties among improving candidates are the only randomness that exists to add; and the tests would need to either seed the randomness or assert membership in the tied set instead of an exact cell. *(Shipped 2026-09-06, M6.b: `flee_step(board, threat, rng)` receives the shared Game RNG through PlayState and chooses randomly among sorted improving candidates. Cornered and single-escape cases consume no randomness; the chooser still does not move Blue. Tests cover tied-set membership and reachability, seeded reproducibility, unchanged RNG state without ties, and the full 600-pairing escape contract across ten seeds. Validation: 55 character tests and 189 full-suite tests passed; a headless PlayState check across 30 seeds confirmed all three open-board flee directions, the shared RNG handoff, and unchanged correct-answer behavior.)*
- **Randomized question rotation within a pool.** M3 hands out questions in deterministic load order — testable, but memorizable. The forcing function is topic selection, and it ships in M4's Topics screen: when the user picks which topics are on the board, a game might run on only 1–3 of them, and a fixed rotation becomes obvious fast. Softened somewhat by the bank surviving replays (used-marks carry over, so rotation continues rather than restarts — see `milestones/m4.md`), but a full app restart still opens identically. Switch `next_question` to a shuffled draw (per-(topic, subtopic)-pool shuffle, reshuffle on recycle); same test strategy as randomized flee — seed it or assert membership. (Distinct from M5.e's *answer* shuffle, which randomizes choice order within one question and is settled, not a knob — this knob is about which question comes next.) *(Shipped 2026-09-06, M6.a: `next_question(topic, subtopic, rng)` receives the shared Game RNG through PlayState. Each pool gets a lazily shuffled order, reshuffled on recycle; usage and order persist across replays. Seeded coverage replaces load-order rotation assertions; canonical loading and answer-choice behavior remain unchanged.)*
- **Randomized cell→category assignment** *(shipped in M4 — settled 2026-08-31)*. Every game start shuffles a balanced cell→pair assignment (pairs cycled to fill the board, then shuffled), driven by an injected `random.Random` so tests can seed it. See `milestones/m4.md`. Question *rotation* (previous entry) is now the only deterministic layer left.
- **Blue's square holds a random category** *(declined 2026-09-06 — see `milestones/m6.md`)*. The planned evolution of the question-gated catch: while Blue stood on a square it would temporarily possess a random (topic, subtopic) pair. Declined after M6.c: hidden possession trades the endgame's informed routing for luck, re-rolling on every flee softens a failed catch, and shuffled rotation already makes the specific question unpredictable. If playtesting shows players herding Blue onto an easy square, draw the label under Blue first (view-only). The underlying goal — a catch that tests more than one known square — is served better by the next entry.
- **Difficulty scales with distance to Blue** *(David's idea, logged 2026-09-06 — depends on a graded bank)*. A mode where the question drawn for a square gets harder as the player closes in: reuse `get_distance` between the player and Blue at ask time — **difficulty 3 at distance 0–1** (the catch and the square beside Blue), **2 at distance 2**, **1 beyond 2**. The pressure curve lands where the game already peaks, the endgame becomes the real test without any new board state, and it's mode/setting material so the flat default survives alongside it. It reads `difficulty`, which today is a placeholder `1` on all 1,595 imports — so this waits on grading (previous entry: grade during playtesting, then this knob starts earning its keep). Design questions to settle when it's built: which distance is measured (player-to-Blue is the natural one; the *clicked* square's distance is the alternative), and what happens when a pool has no question at the wanted tier (fall back to the nearest graded tier rather than crashing, and say so nowhere — the player shouldn't know the bank was thin). Belongs with the move-limit/board-size knobs as the third difficulty lever, and it's the one that transfers to Run from Red unchanged. *(Shipped 2026-09-19, M7.f, as the `distance` tier policy behind the medium and hard presets. Player-to-Blue distance at ask time; wants 3 at 0–1 and 2 at 2 with silent nearest-tier fallback; beyond 2 it wants nothing but filters to tiers 1+2 so the analysis questions are never spent far from Blue. Easy is a policy-wide tiers-1+2 filter. **Known edge, deliberately left (2026-09-19):** a subtopic whose questions are *all* tier 3 gets on the board, is relabeled away at the first far ask because nothing in it is allowed there, and only returns when every pool is dry — so on the distance presets it is effectively never asked. No such subtopic exists in the shipped bank and `tests/test_tier_policy.py` guards that every topic can start on every preset; it will surface the day a T3-only bank or subtopic is written. The fix, if wanted, is to make far-ask exhaustion a preference rather than a relabel trigger.)*
- **Red's movement trigger** (already flagged in §4's turn rules): move-every-turn (relentless) vs move-on-incorrect-only (gentle, mirrors Blue). Deferred until Catch Blue mode is fully playable; this is the main difficulty lever for Run from Red. *(Settled 2026-09-20, David: on incorrect answers only, mirroring Blue — recorded in `game_design.md` §2–3.)*
- **Move-limit difficulty** *(mechanism settled 2026-08-31, number still a knob)*. M4 ships the loss: 20 moves, every answered question — right or wrong, move or catch — costs one. What remains tunable is the number itself, and whether difficulty presets or user-facing fine-tuning ever expose it. Question difficulty and move budget trade off against each other, so tune only with the full flow playable. *(Shipped 2026-09-08, M7.a–b: `Settings.move_limit` rides in `GameConfig`, the Settings screen exposes 10/15/20/30, presets stamp it — easy and medium 15, hard 20 as of the 2026-09-19 retune; `MOVE_LIMIT` is only the default. The number stays a playtest knob.)*
- **Answer feedback on the buttons** *(added 2026-09-01)*. Today the popup resolves inside the click that answers it: `PlayState.handle_events` checks `is_correct`, applies the consequence (move, flee, or the `GameOverState` transition), and tears the popup down — `pending`, `prompt_box`, `answer_buttons` all cleared — before the next frame draws. The player never *sees* which answer was right; they infer it from whether Blue fled. The change: on click, keep the popup up briefly and mark the buttons with a color/border animation — the chosen button flashes green or red, ideally also highlighting the correct answer when the pick was wrong — then resolve. Structurally that's a short *reveal* interlude between answered and resolved: the consequence block (including the end-screen transition on catch or final move) moves out of the click handler and behind a timer, with clicks ignored while the reveal runs. Three things to carry into the build: `Button` currently knows only its own color vs `INACTIVE_BUTTON_COLOR`, so it needs a per-button highlight override; the timer must accumulate in `update` (frame `dt` or `pygame.time.get_ticks()`), never a blocking `pygame.time.wait`, which would freeze the whole loop mid-frame; and the synthetic-click tests assume click → consequence in one step, so the reveal duration wants to be injectable (zero in tests) or the test driver learns to tick past it. For a study tool this is the highest-value knob on the list — "wrong, *and here's the right answer*" is feedback the game currently swallows. *(Shipped 2026-09-06, M6.c: PlayState retains the popup during an explicit answer reveal, advances it through `update(dt_ms)`, and applies consequences only after the configurable duration (900 ms by default; tuned to 1300 ms on 2026-09-06, plus a further 1000 ms on wrong answers from 2026-09-07 — `WRONG_REVEAL_EXTRA_MS`). Buttons support generic highlights; reveal input is ignored, including the expiry frame's event batch. Zero duration preserves immediate resolution for synthetic-click tests. Win-before-loss and shared-RNG flee behavior remain unchanged. Validation: 30 reveal/UI/loop tests and 219 full-suite tests passed without warnings; real-bank headless checks covered menus, timed answers, final-move wins, rendering, and replay continuity across 10 seeds and 20 games. Interactive timing/visual playtesting remains manual.)*
- **Subject → data mechanism** *(settled 2026-08-31 — a `subject` field per question, shipping in M5)*. The topic-from-data principle extended a level up, completing the in-data hierarchy subject → topic → subtopic; `QuestionBank` grows the matching `subjects` / `topics(subject)` rungs. The alternatives lost on drift: a hand-kept subject→topics dict is a second copy of the truth whose failure mode is an invisibly unlisted topic, and subject-as-directory makes location meaningful with nothing inside a misfiled file to check against. Two refinements keep their conveniences without their costs: subject *folders* are welcome as shelves (`data/questions/anatomy_physiology/cells.json` — nominal, not functional; the loader globs everything and the field alone decides), and the menu layer keeps a small *display-name* dict (`anatomy_physiology` → "Anatomy & Physiology", `prettify_topic` fallback) — allowed because it's cosmetic-only: a drifted label costs a label, never reachability. Settled ahead of M5's treadmill so every real question is born with the field. See `milestones/m5.md`.
- **Board size as difficulty.** With Blue starting center, board size scales how much herding a catch requires with no other code changes (5×5 quick, 9×9 patient). If Catch Blue needs difficulty settings, try board size before touching the flee rules. *(Shipped 2026-09-08, M7.a–b: `Settings.board_size` 5/7/9 on the Settings screen, per-size label fonts in the renderer; the 2026-09-19 preset retune puts easy and medium on 5×5 and hard on 7×7, with 9×9 custom-only.)*
- **Difficulty-grading the imported bank** *(deferred 2026-09-01)*. The Anson import landed all 1,595 questions at `difficulty: 1` — a default, not a judgment; nothing in the game reads the field yet. Grade during playtesting, when playing the actual questions shows which ones bite: that's also when the tag starts earning its keep, and it matters most for Run from Red, where question difficulty is a core pressure lever rather than Catch Blue's flavoring. Until then the field is honest scaffolding — don't build difficulty-filtered drawing on top of ungraded data. *(Shipped 2026-09-14, M7.e: every question graded 1–3 on a shared rubric; the loader rejects anything else; `chemical_foundations` and `tissues` have no tier 3 — see the tier column in `question_bank_tracker.md`.)*
- **Board labels re-wrap every frame** *(noted 2026-08-31)*. `BoardView.draw` calls `word_wrap` for all 25 cells on every frame at 60fps, even though the labels are static for the whole game. Harmless at this scale — measured in fractions of a millisecond — and not worth complicating the view over. If profiling during M6 polish (or Run from Red's 81-cell board) ever shows it, the fix is caching: pre-render each label to a surface once when the cell map is built, and blit the cached surfaces in `draw`. Don't do it speculatively; a cache is a second copy of the truth, and the one-map rule in `milestones/m3.md` exists for a reason.
- **Per-mode accent color for the pixel theme** *(David's ask, logged 2026-09-20; lands with Run from Red)*. The pixel look should read blue in Catch Blue, red in Run from Red, green in Race Green, yellow in Find Yellow. Two facts shape the build. First, the Kenney packed sheet in `assets/` is one color family — blue-gray tiles, cream panels, brown accents, plus a few red/green/gray corner pieces — so there is no red or green sheet to swap in; the accent has to be applied at load, by remapping the blue-gray family in the loaded `Surface` to the mode's hue (a per-pixel hue shift on the cropped nine-slice regions, done once when the `Renderer` builds its skin, never per frame). Second, the palette already names the same family in code: `background`, `cell`, `cell_move`, `button`, `button_inactive`, and `highlight` are the blue-gray roles, while `panel`, `text`, and `selected_line` are the cream that should stay put in every mode. So the shape is an *accent* on `Theme` (or a `Theme.with_accent(color)` that returns a `replace`d palette and a tint for the skin), with the four accents as data next to the mode, and the Renderer built from the accented theme when `PlayState` is constructed — the same per-config Renderer that 7×7/9×9 already get (m7.md's open finding), so the seam exists. Menus stay blue; only play and its pause/end screens change. Character colors are a separate question: Red, Green, and Yellow need Okabe-Ito roles beside `player` and `blue` (vermillion, bluish green, and yellow are the palette's own picks), and the accent must keep enough contrast with the character standing on it — check with the WebAIM ratio the way M6.f did. Test with the gallery: one board per mode in the pixel theme, and the flat theme unchanged. *(M8.d, 2026-09-20: this remap is built alongside a second mechanism — whole-skin color variants from a Kenney Pixel UI pack, refactor doc §11a — and the two are compared in the gallery; one ships and the other is deleted. The registry's `accent` is a key that `Game.renderer_for` resolves either way. See `milestones/m8.md`.)*
- **JSON *syntax* errors crash without a filename** *(decided 2026-08-27)*. `QuestionBank`'s loader enriches *shape* errors (missing field, bad index, wrong type) with the offending filename, but a syntactically broken file — stray comma, single quotes — crashes inside `json.load` with a raw `JSONDecodeError`: line and column, no file named. Deliberately left that way: the IDE flags syntax errors live while editing data files, so the gap only bites if a file is edited outside the IDE. If a bare "Expecting ',' delimiter: line 3 column 5" ever appears at startup (M5's content authoring is the likely moment), this is why — the fix is the same catch-enrich-reraise the loader already applies around `from_dict`, wrapped around the `json.load` call instead. *(Shipped: M5.a built exactly this ahead of the treadmill it protects — the loader now names the file on syntax errors, alongside the duplicate-id and near-collision guards; see `milestones/m5.md`.)*
